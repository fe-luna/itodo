type Todo @model @auth(rules: [{ allow: public }]) {
  id: ID!
  uid: String
  name: String!
  description: String
}
type Note @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  description: String
}
